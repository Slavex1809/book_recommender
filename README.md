Система рекомендаций книг
Полный проект системы рекомендаций книг, реализующий несколько подходов машинного обучения для датасета Goodbooks-10k.

📚 Обзор проекта
В этом проекте реализованы различные подходы рекомендательных систем:

Рекомендации на основе популярности - Топ-N самых популярных книг

Контентные рекомендации - TF-IDF + косинусная схожесть по тегам книг

Коллаборативная фильтрация - Упрощенное матричное разложение SVD

Гибридный подход - Комбинация разных методов

book_recommender/
├── data/                    # Загрузка и предобработка данных
│   ├── loader.py           # Функции загрузки данных
│   └── preprocessor.py     # Предобработка данных и EDA
├── models/                 # Модели рекомендаций
│   ├── popularity_model.py # Рекомендации на основе популярности
│   ├── content_model.py    # Контентные рекомендации
│   ├── simple_svd_model.py # Матричное разложение SVD
│   └── evaluator.py        # Метрики оценки моделей
├── notebooks/              # Jupyter ноутбуки для анализа
│   └── analysis.ipynb      # Основной ноутбук анализа
├── figures/                # Сгенерированные графики и диаграммы
├── results/                # Результаты оценки и отчеты
├── cache/                  # Кэшированные данные
├── requirements.txt        # Зависимости Python
├── config.py              # Конфигурация проекта
├── run_project.py         # Основной скрипт запуска
├── install_deps.py        # Скрипт установки зависимостей
├── check_installation.py  # Скрипт проверки окружения
└── README.md              # Этот файл

Быстрый старт
git clone https://github.com/Slavex1809/book_recommender.git
cd book_recommender
# Способ 1: Используя requirements.txt
pip install -r requirements.txt

# Способ 2: Используя скрипт установки
python install_deps.py
Загрузка датасета
Структура папок:
C:\Users\ВашеИмя\
├── book_recommender\   (этот проект)
└── goodbooks-10k\      (папка с датасетом)
    ├── ratings.csv
    ├── books.csv
    ├── tags.csv
    └── book_tags.csv
Запуск проекта
python run_project.py

 Возможности
Реализованные модели
Рекомендации на основе популярности (PopularityRecommender)

Рекомендует самые популярные книги на основе оценок

Простая базовая модель

Нет проблемы холодного старта для популярных книг

Контентные рекомендации (ContentBasedRecommender)

Использует TF-IDF векторизацию тегов книг

Находит похожие книги по содержанию

Решает проблему холодного старта для новых книг

Рекомендации SVD (SimpleSVDRecommender)

Матричные разложения для персонализированных рекомендаций

Упрощенная реализация без внешних библиотек

Учитывает скрытые факторы предпочтений

Анализ данных
Разведочный анализ данных (EDA)

Анализ активности пользователей

Распределение популярности книг

Анализ разреженности матрицы пользователь-книга

Выявление проблемы холодного старта

Метрики оценки
Precision@K - Точность среди первых K рекомендаций

Recall@K - Полнота среди первых K рекомендаций

nDCG@K - Качество ранжирования с учетом позиции

RMSE - Для моделей предсказания оценок

# Загрузка и подготовка данных
from data.loader import DataLoader
from data.preprocessor import DataPreprocessor

loader = DataLoader()
ratings, books, tags, book_tags = loader.load_all_data()

preprocessor = DataPreprocessor()
train_data, test_data = preprocessor.train_test_split_temporal(ratings)

# Обучение моделей
from models.popularity_model import PopularityRecommender
from models.content_model import ContentBasedRecommender

pop_model = PopularityRecommender(min_ratings=50)
pop_model.fit(train_data)

content_model = ContentBasedRecommender(max_features=5000)
books_with_tags = preprocessor.prepare_book_content_data(books, book_tags, tags)
content_model.fit(books_with_tags)

# Получение рекомендаций
user_id = 1
popular_recs = pop_model.recommend(user_id, n=10)
content_recs = content_model.recommend_similar_books(book_id=42, n=10)
